# План TASK-0025

**Статус:** исправление подтверждённой ошибки разрешено пользователем; новый адаптер не реализуется.

1. Сопоставить CLI export с публичным export_state через общий реестр команд переноса.
2. Проверить успешный экспорт, неизменность карточек/истории, ошибки назначения и CLI backup/restore на временных проектах.
3. Запустить тесты пакета и ядра, проверить wheel изолированно и диагностику живого проекта.
4. Обновить документацию и объяснить выбор интерфейса агента, отделив предложение адаптера от реализации.

Основание review: воспроизведённый дефект из отчёта Luna; правка ограничена диспетчеризацией CLI, публичный API и схема не меняются. Разрешение пользователя: «Исправь найденные проблемы».
