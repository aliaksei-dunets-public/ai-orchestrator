# Case: Model Migration

Mode: compare

A GPT-5.4 prompt with extensive prerequisite and verbosity scaffolding is copied unchanged to GPT-5.6. No baseline, eval set, or runtime verbosity configuration is recorded.
