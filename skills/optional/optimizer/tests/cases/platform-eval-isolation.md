# Case: Platform Eval Isolation

Mode: deep

Baseline tests run in developers' normal profiles. User-level memories, global instructions, MCP servers, prior sessions, and write permissions remain enabled. Results differ between machines but only the final answer text is stored.
