# Case: Verbose Agent Output Without an Information Contract

Mode: standard

The target agent is instructed only to "be concise." It still restates the
request, narrates every tool call, repeats unchanged context, includes all
successful logs, creates empty report sections, and provides five alternatives
when one is clearly preferred. Its task analysis is otherwise correct.
